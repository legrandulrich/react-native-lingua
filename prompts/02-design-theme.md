Read AGENTS.md first and follow it strictly.

Implement the full design system using NativeWind based on the attached design theme. Set up global CSS utilities, separate design tokens in theme folder, and font loading/config for the app. Ensure everything is as is shown in attached design theme.

@prompt_material/01-design-system.png

let's go with simpler naming for our utilities. No need type o and two underscore before a specific h one or two. Instead, just make it simpler for me to refer to these utility classes

Explain each generated file for this feature in detail, including the reasoning, architectural choices, and design decisions behin the implementation.

push the code we have so far to a Github repo.

echo "# react-native-lingua" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/legrandulrich/react-native-lingua.git
git push -u origin main